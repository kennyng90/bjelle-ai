{
	"name": "bjelle",
	"private": true,
	"type": "module",
	"packageManager": "pnpm@11.20.0",
	"devEngines": {
		"runtime": {
			"name": "node",
			"version": "24.x",
			"onFail": "download"
		}
	},
	"scripts": {
		"dev": "pnpm -r --parallel --stream dev",
		"build": "pnpm -r build",
		"check": "biome check",
		"check:fix": "biome check --write",
		"deploy:designsystem": "pnpm --filter @bjelle/designsystem run build && pnpm --filter @bjelle/designsystem run deploy",
		"format": "biome format --write",
		"lint": "biome lint",
		"test": "pnpm -r test",
		"test:e2e": "pnpm --filter @bjelle/e2e test:e2e",
		"test:visuell": "pnpm --filter @bjelle/e2e test:visuell",
		"typecheck": "pnpm -r typecheck"
	},
	"devDependencies": {
		"@biomejs/biome": "catalog:",
		"@bjelle/biome-config": "workspace:*",
		"typescript": "catalog:"
	}
}
